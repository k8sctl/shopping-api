apiVersion: v1
kind: Service
metadata:
  name: shopping-app
  namespace: shopping-api
  labels:
    app: shopping-app    # ← 추가
spec:
  type: NodePort
  selector:
    app: shopping-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 32000